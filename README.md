# lineerregression
This code predicts salary from years of experience using a simple linear regression model. It loads a dataset with 'YearsExperience' and 'Salary', constructs the model, and visualizes the results.
